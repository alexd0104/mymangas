<form method="post" action="{{ path('app_manga_delete', {'id': manga.id}) }}" onsubmit="return confirm('Supprimer ?');" style="display:inline">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ manga.id) }}">
  <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
